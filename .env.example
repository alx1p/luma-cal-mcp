# Geocoding provider: nominatim (default, free), google, or mapbox
GEOCODING_PROVIDER=nominatim
GEOCODING_API_KEY=

# Path to SQLite DB for tracking events, preferences, and login state.
# Default: ~/.luma-mcp/events.db
EVENT_STORE_PATH=
